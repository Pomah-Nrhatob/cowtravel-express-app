const { PublishedChapters } = require("../models/models");

class PublishedChaptersController {
  async getOnePublishedChapter(req, res, next) {
    return req;
  }

  async getAllPublishedChapter(req, res, next) {
    return req;
  }
}

module.exports = new PublishedChaptersController();
